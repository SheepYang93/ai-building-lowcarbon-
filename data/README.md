# Data

Place the BDG2-derived daily dataset here as `data/test.csv`.

Raw data are intentionally excluded from Git because of size and provenance considerations. The pipeline filters electricity records for education-related buildings.

Before interpreting `meter_reading` as kWh, verify the exact unit lineage of the derivative dataset.
